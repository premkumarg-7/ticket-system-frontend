export const environment = {
  production: false,
  // Empty string = relative URLs → Angular dev-server proxy forwards to gateway.
  // This avoids CORS issues regardless of which port VS Code forwards the app to.
  apiUrl: '',
};
